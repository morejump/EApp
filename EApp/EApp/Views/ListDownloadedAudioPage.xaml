<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:prism="clr-namespace:Prism.Mvvm;assembly=Prism.Forms"
    prism:ViewModelLocator.AutowireViewModel="True"
    xmlns:controls="clr-namespace:EApp.CustomControl"
    xmlns:fftransformations="clr-namespace:FFImageLoading.Transformations;assembly=FFImageLoading.Transformations"
    x:Class="EApp.Views.ListDownloadedAudioPage">
    <ListView
        x:Name="lstView"
        HasUnevenRows="True"
        ItemsSource="{Binding MyList}">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <controls:ItemOnList
                        hasMoreOptions="True"
                        Title="thao dep trai super man"
                        Description="uperman is a fictional superhero appearing in American comic books published by DC Comics. The character was created by writer Jerry Siegel and artist Joe Shuster"
                        Thumbnail="avatar.png"
                        />
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>

    </ListView>

</ContentPage>